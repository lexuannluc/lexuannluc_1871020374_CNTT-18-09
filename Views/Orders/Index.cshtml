@model List<OrderManagementApp.Models.Order>

<h2>Danh sách đơn đặt hàng</h2>

<!-- ===== THÔNG BÁO ===== -->
@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        @TempData["Success"]
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">
        @TempData["Error"]
    </div>
}

<!-- ===== NÚT THÊM ===== -->
<p>
    <a asp-action="Create" class="btn btn-primary">
        ➕ Thêm đơn hàng
    </a>
</p>

<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>Số đơn hàng</th>
            <th>Tên khách hàng</th>
            <th>Email</th>
            <th>Sản phẩm</th>
            <th>Số lượng</th>
            <th>Ngày đặt</th>
            <th>Trạng thái</th>
            <th>Hành động</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.OrderNumber</td>
                <td>@order.CustomerName</td>
                <td>@order.CustomerEmail</td>
                <td>@order.Product?.Name</td>
                <td>@order.Quantity</td>
                <td>@order.OrderDate.ToString("yyyy-MM-dd")</td>
                <td>
                    @(order.DeliveryDate == null ? "Chưa giải quyết" : "Đã giao")
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@order.Id"
                       class="btn btn-warning btn-sm">
                        ✏️ Sửa
                    </a>

                    <a asp-action="Delete" asp-route-id="@order.Id"
                       class="btn btn-danger btn-sm"
                       onclick="return confirm('Bạn có chắc chắn muốn xóa đơn hàng này không?');">
                        🗑️ Xóa
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
